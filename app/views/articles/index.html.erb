<div class="container">
  <div class="col-sm-8 col-sm-offset-2">
    <% @articles.each do |article| %>

      <h2 class="titre" ><%= link_to article.title, article %></h2>
      <p><%= article.content[0..80] %></p>

    <% end %>
  </div>
</div>
